[pytest]
testpaths = tests
# The submitted code uses Field(..., example=...), which Pydantic v2 deprecated
# in favour of json_schema_extra. It is reported in the write-up as a
# modernisation item; silenced here only so the test output stays readable.
filterwarnings =
    ignore::pydantic.PydanticDeprecatedSince20
