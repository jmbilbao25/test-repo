<?xml version="1.0" encoding="UTF-8"?>
<!--
  Aggregator for the four Spring Cloud applications in this assignment. One
  `mvn package` here builds all of them, and the Spring Cloud BOM is declared
  once so every module agrees on the versions.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.1</version>
        <relativePath/>
    </parent>

    <groupId>com.bilbao.ecommerce</groupId>
    <artifactId>ecommerce-cloud</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>ecommerce-cloud</name>
    <description>Spring Cloud service discovery and configuration, Day 13</description>

    <modules>
        <module>config-server</module>
        <module>eureka-server</module>
        <module>product-service</module>
        <module>order-service</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <spring-cloud.version>2024.0.0</spring-cloud.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
